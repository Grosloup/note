# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{web/.+\.(css|js|html)})
  watch(%r{web/css/.+\.css})
  watch(%r{web/js/apps/\w+/.+\.js})
end
